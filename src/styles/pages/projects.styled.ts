import { styled } from "styled-components";

export const CardsContainer = styled.section`
    padding: 24px;
    display: flex;
    flex-wrap: wrap;
    gap: 64px;
    max-width: 100%;
    height: 100%;
    overflow: scroll;
`